const assert = require("assert");
const docDB = require("../../src/docDB");

describe("docDB", function() {
    before(function() {});
    after(function() {});

    it("should load without throwing an error");
    it("should create new database");
    it("should connect to a db that's already been created");
    it("should throw error if one tries to create a db that already exists");
    it("should delete database");
    it("should clear a database");
});
